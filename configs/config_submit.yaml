ollama:
  host: "http://ollama-gateway:11434"
  model: "granite4:3b"

retrieval:
  chunk_size_en: 384
  chunk_overlap_en: 64
  chunk_size_zh: 384
  chunk_overlap_zh: 64
  top_k: 8
  debug: false
  
  # ... (weights logic remains, though handled by Ensemble internally now) ...

  # Parent Document Retrieval
  parent_document_retrieval:
    enabled: false # Disabled for final optimized run
